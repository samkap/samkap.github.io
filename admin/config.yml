# config.yml

backend:
  name: git-gateway
  branch: master # Branch to update (optional; defaults to master)
media_folder: 'img'
collections:
  - name: 'posts'
    label: 'posts'
    folder: '_posts/'
    fields:
      - { name: Title }
